<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.edu.utas.asornob.raffledrawingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddCustomer"></activity>
        <activity android:name=".SelectedRaffle" />
        <activity android:name=".TicketSaleConfirm" />
        <activity android:name=".CustomerCreate" />
        <activity android:name=".CustomerSelect" />
        <activity android:name=".TicketSale" />
        <activity android:name=".CreateRaffle" />
        <activity android:name=".ActivityRaffleList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>