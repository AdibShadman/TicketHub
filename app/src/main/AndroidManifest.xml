<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.edu.utas.asornob.raffledrawingapp">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditRaffle"></activity>
        <activity android:name=".SelectedRaffle" />
        <activity android:name=".TicketSaleConfirm" />
        <activity android:name=".CustomerCreate" />
        <activity android:name=".CustomerSelect" />
        <activity android:name=".TicketSale" />
        <activity android:name=".TicketList" />
        <activity android:name=".CreateRaffle" />
        <activity android:name=".ActivityRaffleList" />
        <activity android:name=".CustomerDetails" />
        <activity android:name=".CustomerEdit" />
        <activity android:name=".WinnerList" />
        <activity android:name=".DrawMargin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="au.edu.utas.asornob.raffledrawingapp"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>